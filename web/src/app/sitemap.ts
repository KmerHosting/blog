import type{MetadataRoute}from'next';import{getArticles}from'@/lib/strapi';export default async function sitemap():Promise<MetadataRoute.Sitemap>{const a=await getArticles().catch(()=>[]);return[{url:'https://blog.kmerhosting.com',lastModified:new Date(),changeFrequency:'weekly',priority:1},...a.map(x=>({url:`https://blog.kmerhosting.com/articles/${x.slug}`,lastModified:x.publishedAt?new Date(x.publishedAt):new Date(),changeFrequency:'monthly' as const,priority:.8}))];}